name: Release binaries

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  linux:
    name: Linux static (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runs-on: ubuntu-latest
          - arch: aarch64
            runs-on: ubuntu-24.04-arm
    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v5

      - name: Build static oi inside alpine container
        # Mirrors Dockerfile.oi (as generated by `oi registry docker`).
        # [ocaml-option-static] configures the ocaml switch to emit fully
        # static binaries; no ld flag fiddling required.
        run: |
          cat > Dockerfile.build <<'EOF'
          FROM ocaml/opam:alpine-3.22-ocaml-5.4 AS builder
          USER root
          RUN apk add --no-cache build-base m4 perl pkgconf git curl bash patch \
              gmp-dev sqlite-dev sqlite-static openssl-dev openssl-libs-static \
              zlib-dev zlib-static
          USER opam
          WORKDIR /home/opam/src
          COPY --chown=opam:opam . /home/opam/src
          ENV OPAMYES=true OPAMCONFIRMLEVEL=unsafe-yes
          RUN cd ~/opam-repository && git pull --quiet origin master \
              && opam-2.5 update default
          RUN opam-2.5 switch create . \
              --packages "ocaml-option-static,ocaml-option-no-compression,ocaml.5.4.1"
          RUN opam-2.5 exec -- dune build bin/main.exe
          USER root
          RUN install -m 0755 _build/default/bin/main.exe /oi
          FROM scratch
          COPY --from=builder /oi /oi
          EOF
          mkdir -p dist
          docker buildx build \
            -f Dockerfile.build \
            --output type=local,dest=./bin-out \
            .
          install -m 0755 bin-out/oi dist/oi-linux-${{ matrix.arch }}

      - uses: actions/upload-artifact@v4
        with:
          name: oi-linux-${{ matrix.arch }}
          path: dist/oi-linux-${{ matrix.arch }}
          if-no-files-found: error

  macos:
    name: macOS release (arm64)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.4.1"

      - name: Build release oi
        run: |
          opam install -y --deps-only .
          opam exec -- dune build --profile=release bin/main.exe
          mkdir -p dist
          cp _build/default/bin/main.exe dist/oi-macos-arm64
          chmod 755 dist/oi-macos-arm64

      - uses: actions/upload-artifact@v4
        with:
          name: oi-macos-arm64
          path: dist/oi-macos-arm64
          if-no-files-found: error

  release-upload:
    name: Attach binaries to release
    needs: [linux, macos]
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Upload binaries to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            dist/oi-linux-x86_64 \
            dist/oi-linux-aarch64 \
            dist/oi-macos-arm64 \
            --clobber
